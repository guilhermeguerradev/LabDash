version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: labareda_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - labareda_data:/var/lib/postgresql/data
    restart: always
    networks:
      - labareda-network

  pgadmin:
    image: dpage/pgadmin4
    container_name: labareda_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD}
    ports:
      - "5050:80"
    depends_on:
      - db
    restart: always
    networks:
      - labareda-network

volumes:
  labareda_data:

networks:
  labareda-network:
    driver: bridge
